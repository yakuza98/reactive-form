import {setInputCase} from "../helpers/helpers";

export enum Frameworks {
  angular = 'angular',
  react = 'react',
  vue = 'vue',
}
export enum Messages {
  openSnackBarMessage = 'The form has been submitted',
  errorSnackBarMessage = 'Sorry, something went wrong',
  enterDurationMessage = 'ENTER DURATION OF YOUR HOBBY',
}
export enum MessagesAction {
  close = 'Close'
}

export enum InputCases {
  first = 'first',
  lower = 'lower',
  upper = 'upper',
}
